<ul id="sidebar-content" class="collapse nav nav-pills nav-stacked" data-hook="admin_content_sub_tabs">
  <% SpreeEssentials.essentials.each_pair do |name, cls| %>
    <%= tab *cls.sub_tab if defined?(cls.sub_tab) %>
  <% end %>
  <%= tab :uploads, :label => I18n.t('spree.admin.subnav.uploads'), :match_path => "/uploads" %>
</ul>
